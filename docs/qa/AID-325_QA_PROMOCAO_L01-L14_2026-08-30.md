# AID-325 — QA pós-coorte: regressão do lote l01–l14 na promoção (PR #182, pin `6d72735b`)

**Data:** 2026-08-30 UTC (~15:20–15:50) · **QA independente:** `ca6a3f95-8572-43f4-822a-6b40b9bdb63b`
(contexto separado do produtor fa8130d5/AID-343) · **Veredito: NO-GO** por 1 defeito S1 de
identidade da promoção (D1 abaixo). **Toda a matriz funcional do lote PASS** no conteúdo do pin
(`6d72735ba2113cb6198c4d7be26c2f01e5f5d694`, merge do PR #182).

## Registro da promoção verificada (AID-343, RELEASE.md 15:32Z)

| Campo | Registrado pelo produtor | Observado pela QA (~15:35–15:48Z) |
| --- | --- | --- |
| `sourceRevision` | `6d72735b…` (merge PR #182, ref `release/6d72735b`) | idem (alias e permalink) ✔ |
| Manifesto (SHA-256) | `53aa5c52…` | permalink `53aa5c52…` ✔ · **alias `7d0e16d9…` ✘** |
| Deploy vigente | `6a944c43b551c9d76593a978` | permalink serve exatamente o registro ✔ · **alias não** ✘ |
| `os` index | `d2c8912c…` | permalink ✔ · **alias `49d7cc8a…`** ✘ |

## D1 (Sev 1 — identidade da promoção): alias serve build não registrado com binding externo extra

**Evidência executável** (artefatos em `aid325-evidence/`, workspace QA):

- `pilot-bundle-manifest.json`: alias SHA-256 `7d0e16d9902e6a1f4b667db8bc8c8525f5cbdd33e3fd5d55e85a38c5fae836fe`
  ≠ permalink/draft `53aa5c522c27ae77…` (3 fetches consecutivos, estável 15:35–15:48Z; CDN `age` baixo).
  Ambos declaram `sourceRevision 6d72735b…`.
- OS bundle: alias `assets/index-uKi6IxRi.js` (368.411 B) ≠ permalink `assets/index-DAW9RPIG.js` (368.158 B).
  Chunk de missões idêntico após canonização de nomes (120.828 B ambos) — **o delta está no bundle principal**.
- **Delta semântico único nos env embutidos** (`env-diff.txt`): o build do alias contém
  `VITE_PIXELDOJO_URL=https://6a920159a8d5e2dfdd7fbeca--singular-crostata-273e7e.netlify.app/`
  (site **externo** ao host canônico, vivo HTTP 200). O build registrado NÃO contém essa variável.
  Essa variável liga o app `engines` do OS (`registry.ts:92`, `EngineHubApp.tsx:57`) a um app pixelDojo
  externo — superfície que não fez parte da revisão do PR #182 nem do registro da promoção.
- Causa raiz (alta confiança, mecanismo confirmado no `netlify.toml` @ pin): o site tem **build CI**
  (`npm run build:pilot`) que injeta `VITE_*_URL` "configuradas no provedor" (comentário do próprio
  netlify.toml). O deploy CLI do FPE (`6a944c43`, build local sem a env) foi publicado ~15:31Z e
  registrado; um build CI do mesmo ref (com env do provedor) foi publicado em seguida
  (entre 15:28:18Z — alias ainda `3f641906` — e ~15:35Z) e **tomou o alias**. Confirmação do
  rascunho do FPE: draft `6a944b82` == registrado `53aa5c52` (o build local é determinístico; o do CI não é byte-idêntico ao local).
- Janela observada ao vivo: 15:26:48–15:28:18Z alias = `3f641906`; ~15:36Z em diante alias = build `7d0e16d9`.

**Impacto:** aprendizes veem bytes que nunca foram registrados/verificados; o controle central do
decreto AID-323 ("identidade é o controle") falhou na primeira promoção pós-coorte. Mitigação
observada: todas as jornadas críticas PASSARAM também no build não registrado (matriz abaixo), e a
exposição ao pixelDojo externo requer navegação deliberada em `/desktop` → apps → engines (não
encontrada na jornada hub→missão→mapa). Severidade mantida S1 pelo princípio de identidade e pela
superfície externa não revisada.

**Ação requerida (owner FPE fa8130d5):** (1) republicar o build registrado `53aa5c52`/`6a944c43` no
alias OU registrar formalmente o build CI como canônico (com revisão da superfície externa
pixelDojo); (2) neutralizar o canal concorrente (desligar auto-publish CI do branch de produção ou
alinhar env do provedor ao contrato same-origin); (3) re-registrar e pedir re-QA de identidade
(child issue aberta). Rollback disponível: `6a923bf2` (`3f641906`).

## Matriz de regressão AID-278/284 (conteúdo do pin `6d72735b`)

| Charter | Resultado | Detalhe |
| --- | --- | --- |
| 1 identidade | **FAIL (D1)** | alias≠permalink≠registro (acima). Dentro do esperado: `sourceRevision`==pin nos dois; inventário autoss consistente 8/8 no alias; legais byte-a-byte == git @ pin (`385d87d6…`/`27fe5a37…`) em alias e permalink; permalink == registro byte-a-byte (7/7 inventário próprio + manifesto `53aa5c52`) |
| 2 amostragem l04–l14 | **PASS 69/69** | 11 missões novas l04–l14 (desktop/alias): iframe `/apps/literacydojo/` same-origin, `MOTOR running`, sem mismatch de contentVersion, `engine.ready` `2026-08-21.1`. Controles l01–l03 idem. Permalink l14 idem. Mobile l01+l14 idem. Mapa: `17 missões, uma sequência` (contagem derivada do catálogo; `MapScreen.tsx:56`). Dev `game-02-warehouse`: iframe same-origin, MOTOR running, HUD `role=status`/`aria-live=polite`. (1 FAIL inicial era artefato de sonda na página errada — desmentido por probe direto) |
| 3 smoke remoto + guardas | **PASS 20/20 + ponte OK** | 5 superfícies 200; OS embute literacy same-origin sem pin stale; projeção publica l04–l14; literacy `2026-08-21.1` (não `2026-07-25.1`); gate l01–l14 presente no literacy; reflow @320 retido (`docScrollW=298 ≤ innerW=298` em l01 e l14); ponte: session same-origin **200** token 43 chars / cross-origin **403** `origin-forbidden` / POST sem token **401** (assinatura D1-AID-282/AID-301 retida). Guardas locais @ worktree do pin: substrate **196 passed** (inclui invariante contentVersion de bindings), `pilot-bundle-lib` **20/20**, bridge netlify **0 fail** |
| 4 fronteira canônica | **PASS** | `learner/learning_state.yaml` `c3cae54c…` e `.mavis/learning_state.yaml` `a900918a…` antes==depois (iguais a AID-313/366); blob git de `learner/learning_state.yaml` idêntico `29b59a92`→`576d4a57`→`6d72735b` (confirma independentemente o claim do produtor). Zero escrita canônica por esta QA |

## Achado P3 (pré-existente, não-regressão do lote): `learner.substrate --check` nunca fica verde em árvores antigas

`python3 -m learner.substrate --check` no merge reporta DRIFT em 4 projeções
(`engines/minimaxDojo/whiteboard/{profile.yaml,learner_profile.md,trail.md}`,
`engines/voxelDojo/shared/content.ts`). Conjunto **idêntico** no pai `576d4a57` e no pin da coorte
`3f641906`; causa é staleness de relógio (campos `updated:` e `dueIn: "overdue Nd"` derivam da data
corrente — confirmado re-gerando em cópia descartável: só mudam timestamp/contadores). Não é
regressão do PR #182 nem bloqueia o pin; exige follow-up de tooling (projeções com valores de
relógio tornam o `--check` estruturalmente instável).

## Resultado consolidado

| Item | Estado |
| --- | --- |
| Lote l01–l14 funcional no pin promovido | **PASS** (missões novas + controles + mapa + Dev + ponte + reflow + legais) |
| Fronteira canônica | **PASS** (zero escrita; blobs idênticos na linhagem) |
| Identidade da produção (alias==permalink==registro) | **FAIL — D1 S1 → NO-GO** até republicação/re-registro |

**NO-GO** para declarar o lote l01–l14 "promovido com identidade verificada". O conteúdo do pin é
funcionalmente são; o bloqueio é exclusivamente o defeito de identidade/promoção (D1), cuja correção
e re-verificação mínima estão delegadas na child issue (owner FPE; re-QA de identidade pela QA).

## Limitações

- Chromium headless apenas (padrão AID-254/297/301/313); sem NVDA/VoiceOver nesta fatia.
- Deploy id exato do build não registrado no alias não é observável de fora do Netlify (cabe ao FPE);
  a prova de divergência é byte-a-byte (manifesto/assets/env) entre alias, permalink e registro.
- Amostragem mobile limitada a l01/l14 (spot cells, padrão AID-284).
- `--check` do substrate reporta DRIFT pré-existente (P3 acima) — critério forte desta QA é o hash
  canônico antes==depois, que passou.

## Artefatos (workspace QA `ca6a3f95`, `aid325-evidence/`)

`qa325-identity.{mjs,log}` · `qa325-missions.{mjs,log}` · `qa325-smoke.{mjs,log}` ·
`bridge-probes.log` · `boundary-before.txt` / `boundary-after.txt` · `manifest-alias-3f641906-prepromocao.json` ·
`m_alias.json` / `m_perma.json` (manifestos divergentes) · `osjs_alias.js` / `osjs_perma.js` (bundles OS divergentes) ·
`env-diff.txt` · `run-timestamp.txt` · `shots/` (l04/l09/l14 desktop, map, desktop/enginehub).
