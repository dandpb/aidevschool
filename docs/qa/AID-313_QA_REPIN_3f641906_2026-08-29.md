# AID-313 — QA independente: verificação do re-pin `3f641906` (AID-305, ponte Dev restaurada)

**Data:** 2026-08-29 UTC (~02:00–02:20; duas execuções QA — run parcial 01:59–02:08 e re-execução
completa 02:08–02:20, resultados idênticos)
**QA independente:** `ca6a3f95-8572-43f4-822a-6b40b9bdb63b` (contexto separado do produtor
fa8130d5). **Disposição: GO** para o pin `3f641906` como identidade técnica da coorte (AID-180),
condicionado aos gates humanos que seguem com o CEO (confirmação reconciliada, moderador,
convites). Achado único P3 de ferramenta (spec do PR #178) registrado como follow-up — não afeta
o pin.

## Identidade verificada

| Campo | Observado | Esperado (AID-305/AID-313) |
| --- | --- | --- |
| Manifesto alias (SHA-256) | `fc694824d22e0dbd0def7be6c284ce37d8274342df34e3855eb03c4fc6c7d658` | confere |
| Manifesto permalink `6a923bf2` | idêntico byte-a-byte ao alias | confere |
| `sourceRevision` | `3f6419063e1dad923317c911227a8b21fcf50ad7` | confere |
| `os` index.html | `4658412b…` == manifesto | confere |
| Ref `release/3f641906` no origin | `3f641906` (commit) | confere |
| Linhagem (merge-base --is-ancestor) | `bbf27bb5`, `7426d384`, `29b59a92`, `7cd393d9`, `afd6789`, `61b85535`, `ec265fab` — 7/7 ancestrais | cadeia GO retida |
| PR #181 (bridge in-repo) | head `7cd393d9`, **aberto, NÃO mergeado em main** | governança AID-293 respeitada |

Delta estático vs `bbf27bb5` (deploy `6a923b59`): manifestos comparados — `surfaces` e `files`
idênticos; único campo diferente = `sourceRevision`. Evidência AID-308 (axe 0 serious/critical,
contraste #1f4034) transfere por byte-identity.

## Charter 1 — Identidade/linhagem/inventário: **PASS**

```text
curl alias+permalink /pilot-bundle-manifest.json -> sha256 fc694824… (idênticos; sourceRevision 3f641906)
git merge-base --is-ancestor {bbf27bb5,7426d384,29b59a92,7cd393d9,afd6789,61b85535,ec265fab} 3f641906 -> 7/7 OK
Inventário 27/27 arquivos do manifesto: HTTP 200 alias+permalink, byte-idênticos, SHA-256 == manifesto
Legais byte-a-byte: termos 385d87d6… / privacidade 27fe5a37… (== manifesto, alias==permalink)
```

## Charter 2 — Ponte Dev (foco F1): **PASS**

### Sonda HTTP (curl, alias)

| Probe | Resultado |
| --- | --- |
| `GET /__dojo/bridge/v1/session` + `Sec-Fetch-Site: same-origin` | **200 application/json**, token 43 chars |
| idem sem header (cross-origin) | **403 JSON `{"error":"origin-forbidden"}`** (assinatura AID-219/AID-301) |
| idem `Sec-Fetch-Site: cross-site` (permalink) | **403** idem |
| `POST /verification` cross-origin | **403** origin-forbidden (gate de origem antes da auth) |
| `POST /verification` same-origin sem token | **401 unauthorized** |
| `POST` same-origin + header `x-codexdojo-bridge-token` + schema errado | **422 unsupported-schema** |
| `POST` + token + registro WAREHOUSE L1 sintético 5/12 | **200 recibo FAIL** (`independent_pass:false`, claim coerente) |
| `POST` + token + registro 12/12 | **200 recibo PASS**, `canonical_gate_status:not-submitted`, `producer_writes_mastered:false`, digest correlacionado |

### Jornada Dev em browser (Chromium headless limpo, 1280×800, alias; matrix 3 viewports no run 1)

FAIL→retry→PASS em `game-02-warehouse`; tráfego de ponte capturado:
`session 200 JSON`; **2 POSTs de verificação → 2 recibos 200** (nenhum perdido):
`FAIL attempt kv-warehouse-L1-attempt-1` e `PASS attempt kv-warehouse-L1-attempt-2`.
Digests recomputados independentemente (stable-stringify canônica, `ts`/`verifier` excluídos):
`095fd2a0…` (FAIL) e `7648ecf6…` (PASS) — **ambos == receipt.evidence_digest** (correlação
byte-level com o registro enviado pelo cliente). Header `x-codexdojo-bridge-token` presente nos
POSTs. Host exibe **"Verificação independente aprovada"** / "O verificador independente aprovou
esta evidência. O gate canônico continua separado." — **sem** 'Verificador indisponível',
'Temporariamente indisponível' ou 'Evidência rejeitada'. F1/AID-301 resolvido em produção.

Função canônica `learner/gate/netlify-functions/dojo-verification-bridge.mjs` @ `7cd393d9`:
sha256 `ce72a04f…` (git). `netlify.toml` @ `3f641906`: `functions = "netlify/functions"`
dentro do site root; redirects `session`/`verification` à frente do fallback SPA. Staged é
projeção gitignored por design; guarda `verifyStagedVerifier()` bloqueia deploy com drift
(conferida no código @ `3f641906`) e o comportamento runtime reproduz o contrato canônico
exatamente (matriz de 12 chaves fechadas + digests).

## Charter 3 — Superfícies/guardas: **PASS (1 achado P3 de ferramenta)**

- Pre-check do produtor re-executado de forma independente (`QA_BASE_URL=alias`):
  **21/21 PASS** (identidade, superfícies 200, MOTOR l01/l02/warehouse same-origin,
  contentVersion 2026-08-21.1, predicados reflow @320 e @298 `docScrollW=298 ≤ innerW=298`
  — fix AID-271 retido).
- Guardas de tooling no worktree do pin: `node --test scripts/pilot-bundle-lib.test.mjs`
  **20/20**; `node --test learner/gate/tests/dojo_verification_bridge_netlify.test.mjs` **2/2**.
- Contraste AID-298: manifesto `3f641906` == `bbf27bb5` em `surfaces`+`files` (byte-identity,
  único delta `sourceRevision`) → **AID-308 PASS** (axe 0 serious/critical em alias+permalink)
  vale para esta identidade.
- Smoke remoto (spec do PR #178, wt `1ecfbd1a`@06081f3, `QA_BASE_URL=alias`):
  IA Prática l02 **3/3 PASS** (desktop/tablet/mobile); Dev game-02 **3× conclusão de rodada
  OK**, porém a asserção final da spec falha porque ela **assere a degradação antiga**
  (`Verificador indisponível` / `Temporariamente indisponível` — comportamento spec-locked do
  pin `29b59a92`, ver AID-289/AID-301). No pin `3f641906` a ponte está viva (decisão CEO F1) e o
  host reporta aprovação — o elemento degradado não existe. **Não é defeito do pin**; exige
  atualização da spec remota do PR #178 antes do merge (follow-up aberto, P3).

## Charter 4 — Fronteira canônica: **PASS**

```text
sha256(learner/learning_state.yaml) = c3cae54c… (antes == depois; == AID-180/AID-254/AID-258/AID-301)
sha256(.mavis/learning_state.yaml)  = a900918a… (antes == depois)
```

Nenhuma escrita em `learner/`/`.mavis/` por esta verificação. Sondas sintéticas usam a ponte
(stateless; apenas recibos) — não tocam estado canônico.

## F2 (entrega AID-305) — reconciliação documental: **PASS**

`work-products/AID-180/INITIAL_CONTROLLED_COHORT.md` aponta exclusivamente para
`3f641906`/`6a923bf2`/`fc694824` (permalink e alias corretos; blocker 1 referencia a identidade
reconciliada e marca as confirmações `ec265fab` como superseded; blocker 2 = GO/NO-GO da QA sobre
esta identidade — atendido por este documento; identidades anteriores marcadas superseded/históricas).

## Achado

### P3 (ferramenta, não-produto) — spec remota do PR #178 trava a degradação antiga

`tests-remote/release-journeys.remote.smoke.spec.ts` (branch PR #178, `06081f31`-era) assere
`Verificador indisponível` + `Temporariamente indisponível` no fim da jornada Dev — válido para
pins sem ponte (AID-289/AID-301), falso para `3f641906+` com a ponte restaurada (F1). A spec
precisa tornar-se sensível ao estado da ponte (aprovação quando viva; degradação quando ausente)
antes do merge do PR #178. Owner: FPE fa8130d5 (produtor do PR #178). Não bloqueia o GO deste pin
— o comportamento novo correto foi verificado independentemente (probe dedicado + matrix 3
viewports, ambos os runs).

## Resultado consolidado

| Charter | Resultado |
| --- | --- |
| 1 identidade/linhagem/inventário | PASS (27/27; 7/7 ancestrais; alias==permalink==fc694824) |
| 2 ponte Dev com recibo | PASS (200/403/401/422; FAIL→retry→PASS; digests correlacionados ×2; sem degradação) |
| 3 superfícies/guardas | PASS (precheck 21/21; guardas 20/20+2/2; reflow retido; contraste por byte-identity; smoke: 3/3 IA Prática + 3× rodada Dev OK com achado P3 de spec) |
| 4 fronteira canônica | PASS (c3cae54c/a900918a inalterados) |
| F2 doc coorte | PASS |

**GO para `3f641906`/deploy `6a923bf2`/manifesto `fc694824`** como identidade técnica da coorte
(AID-180). O GO fecha a condição técnica da confirmação reconcilada (`17956ad1`) e de
AID-305/AID-304; gates humanos (aceite AID-142 `35fd67c0` reconciliado, moderador, convites)
seguem com o CEO. Rollback elegível se necessário: `6a923b59` (bbf27bb5) ou `6a9227f6` (29b59a92).

## Limitações

- Chromium headless apenas (padrão AID-254/AID-297/AID-301); sem NVDA/VoiceOver nesta fatia.
- Bytes da função implantada não são servidos estaticamente; a igualdade staged==canônico é
  garantida pela guarda de deploy (código conferido no pin) + paridade comportamental byte-level
  (digests, contrato de 12 chaves fechadas, gating de origem) — não por fetch direto.
- Smoke 6/6 literal não reproduzível com a spec vigente do PR #178 (achado P3): a fatia Dev foi
  comprovada por probe independente + matrix de viewports em vez da asserção degradada da spec.

## Artefatos (workspace do agente QA)

`aid313-evidence/` (workspace ca6a3f95): manifestos alias/permalink, bridge-probes.txt,
bridge_verify_matrix.json, digest_correlation_final.json, aid313-journey.{log,json},
dev-journey-results.json, dev-viewport-matrix.{mjs,json,log}, probe-dev-journey.mjs,
inventory.txt/inventory27.json, precheck-bridge.log, remote-smoke-6of6.log,
boundary-before/after.txt, shots/.
