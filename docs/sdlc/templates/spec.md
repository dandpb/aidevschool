# Spec: <one-line title>

Change-id: <same as intent> · From: intent/<change-id>/intent.md · Status: draft | accepted

## Requirements

Numbered, testable statements. Each open question from the intent is answered
here or explicitly carried forward with an owner.

## Design

How the requirements map onto this repo: components touched, data/state flow,
learner-state impact (if any), derived views that must be regenerated
(`python3 -m learner.substrate`) and contracts honored
(`docs/design/teaching-game-contract.md`, ecosystem MANIFEST, …).

## Policy applied

Which conventions/skills governed the decisions (AGENTS.md rules, engine
AGENTS.md, learning-gate rules). Note versions/commits relied on.

## Flagged concerns

Where policies conflict, where certainty is low, and what an analyst would
have escalated. Each concern names its owner (policy owner / tech lead).

## Out of scope

Explicitly not doing in this change.
