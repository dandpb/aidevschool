# AID-343 — Evidência datada: promoção do lote l01–l14 para produção (pin `6d72735b`)

**Data (UTC):** 2026-08-30T15:32Z (1ª promoção) · **correção de corrida 16:1xZ** · **Produtor:** FPE `fa8130d5` · **Ordem:** CEO AID-393 (sweep AID-392) · **Registro completo:** `work-products/AID-343/RELEASE.md` · **QA independente pendente:** AID-325 (QA Lead `ca6a3f95`)

## Identidade promovida (FINAL — verificada ao vivo pós-correção)

| Campo | Valor |
| --- | --- |
| `sourceRevision` (pin) | `6d72735ba2113cb6198c4d7be26c2f01e5f5d694` (merge PR #182; pais `576d4a57`+`5ee8b27b`) |
| Ref imutável | `refs/heads/release/6d72735b` (ls-remote: idêntica ao pin; confirmação 2026-08-30T15:2xZ e 16:0xZ) |
| Manifesto sha256 | `7d0e16d9902e6a1f4b667db8bc8c8525f5cbdd33e3fd5d55e85a38c5fae836fe` |
| Deploy produção | **`6a944cf24d75848dee3a5505`** · alias `aidevschool-codexdojo-os.netlify.app` · permalink `6a944cf24d75848dee3a5505--aidevschool-codexdojo-os.netlify.app` |
| Deploy draft verificado | `6a944c64b8459d23d276d492` (superseded pela promoção dos mesmos bytes) |
| Superseded (corrida) | `6a944c43b551c9d76593a978` (manifesto `53aa5c52…` — build SEM `VITE_PIXELDOJO_URL`; ver §Corrida) |
| Superseded → rollback elegível | `6a923bf25bc97ecfacfd3fed` (`3f641906`, pin da coorte) |

**Identidade pós-pin:** alias == permalink == build local (sha256 do manifesto conferido ponto a ponto; precheck 30/30 nos dois endereços, `work-products/AID-343/precheck-final.mjs`). Ponte same-origin: 200 token 43ch com `Sec-Fetch-Site: same-origin`; 403 `{"error":"origin-forbidden"}` sem header; 401 sem token. Superfícies 200. MOTOR ao vivo: l01/l02/l04/l14 + warehouse `MOTOR running`, iframes same-origin. Catálogo: 17 bindings (l01–l14 + 3 jogos Dev); copy runtime "17 missões, uma sequência". OS embute o pin imutável do pixelDojo (`6a920159…--singular-crostata-273e7e.netlify.app`). Reflow AID-271 @320/@298 e contraste AID-298 (via PR #179/#180) presentes no candidato.

## Correção de corrida de release (16:1xZ)

Dois runs do FPE promoveram o MESMO pin em paralelo (wake `issue_blockers_resolved` + run AID-393 Parte 2). A 1ª promoção (`6a944c43`, 15:32Z) foi construída por `npm run build:pilot` — entrada de preview que não define `VITE_PIXELDOJO_URL` — deixando o OS com fallback de desenvolvimento no lugar do pin imutável (contrato de `scripts/build-pilot-bundle.mjs`). A 2ª execução rebuildou pelo entrypoint de deploy correto e re-pinou a produção (`6a944cf2`, manifesto `7d0e16d9…`), com o pin pixelDojo verificado no bundle servido. `6a944c43` superseded (permalink retido). QA AID-325 deve verificar SOMENTE `6a944cf2`/`7d0e16d9…`.

**Canônico:** zero escrita em `learner/`/`.mavis/` pela promoção; hashes antes/depois idênticos e iguais ao baseline AID-254 (`c3cae54c…`/`a900918a…`). Achado pré-existente em `main` (drift de 4 projeções no `--check`, conjunto idêntico no pai do merge e no pin da coorte `3f641906`) registrado como follow-up AID-396 — não introduzido pela promoção.

**Correção de SHA da ordem AID-393:** o SHA citado (`…a211cb61…`) contém transposição; o objeto válido/head de main é `…a2113cb6…` (par merge/head-de-branch `6d72735b`/`5ee8b27b` confere com a ordem).

Rollback owner: FPE. Próximo gate: QA AID-325 (produtor ≠ verificador).
