{
  "schemaVersion": 1,
  "observerContext": "independent-readiness-observer",
  "gitSha": "289cdbd87b45ecf0a1f2d63514ebb9a4897fa174",
  "observedAt": "2026-09-04T02:55:00Z",
  "scenarios": [
    {
      "scenarioId": "dojotoday-active-unit-guidance",
      "outcome": "pass",
      "gaps": [],
      "assertions": [
        {
          "id": "learner-understands-guidance-only",
          "evidence": "observation",
          "outcome": "pass",
          "notes": "First-hand QA observation 2026-09-04 02:55-02:56Z in a dedicated clean worktree at the pin (chromium over `vite preview` of the production build; read model src/data/today.ts asOf 2026-08-21 as committed, 17.64 kB bundle). The page renders 'Sua lição de hoje' with the FSRS list (ATRASADA overdue 4d/43d markers), the highlighted active unit (KV WAREHOUSE, 'Retome por aqui') and Sócrates guidance: 'Comece pelo desafio em curriculum/02_key_value_store/docs/spec.md. Primeiro você tenta — quem avalia a evidência é o verificador independente, não eu.' Mastery framing appears only as verified-gate facts ('dominadas … verificadas por gate', main.ts:164-165) and the surface states it 'apenas mostra o scheduler — não marca mastered' (main.ts:232). The only interactive controls are play-inline, ask-tutor and the optional assistant config (explicitly experimental, key kept in this browser only, conversations not saved, off by default) — no grading or completion control exists on the surface (screenshot ev/dt-1-today.png). Post-#264 hosted route also observed at the same pin: `?host=os` renders 'Sugestão neste dispositivo / NÃO É FSRS CANÔNICO' and states 'Esta vista lê o progresso local do OS. Não escreve learner/learning_state.yaml e não marca mastered' (screenshot ev/dt-2-host-os.png). Observer response: I know what to study today and this surface guides without evaluating."
        }
      ]
    },
    {
      "scenarioId": "dojotoday-read-only-boundary",
      "outcome": "pass",
      "gaps": [],
      "assertions": [
        {
          "id": "regeneration-preserves-read-only-boundary",
          "evidence": "document-review",
          "outcome": "pass",
          "notes": "Document/code review at 289cdbd8: src/data/today.ts is headed 'AUTO-GERADO por learner/substrate/adapters/dojotoday.py — NÃO EDITAR À MÃO. Fonte: learner/learning_state.yaml + scheduler learner.substrate.scheduling' (regenerated 2026-08-21); regeneration is the substrate-owned prebuild (`python3 -m learner.substrate` in package.json prebuild), a read-only projection. Write-path audit of engine src/: the only persistence is localStorage for the OPTIONAL assistant config (assistant.ts:30-44, user's own key/endpoint, off by default). Post-#262/#264 sink review requested by the verdict: main.ts:341 assigns `root.innerHTML = renderLocalSuggestion(await loadHostLocalToday())` on the hosted route, and renderLocalSuggestion now routes title/detail through escapeHtml (localProjection.ts:107-108), so hostile OS progress cannot inject markup — the AID-753 XSS chain is closed at the sink, and my own hosted-os.spec.ts unit runs at the pin confirm hostile title/detail are escaped and hostile OS progress never leaks raw payload. The OS projection itself is read-only: readOsProgress opens the OS IndexedDB in a 'readonly' transaction and aborts onupgradeneeded, so dojoToday never even creates that database (localProjection.ts:60-87); its UI repeats 'Não escreve learner/learning_state.yaml e não marca mastered' (localProjection.ts:109). Canonical boot renders only the committed read model (main.ts:344 render(today)). No code path writes learner/ or marks mastery; selfcheck green in my run ('OK: dojotoday self-check passed (track + game_dir)') and the green playwright readiness run complete the boundary proof at the pin."
        },
        {
          "id": "stale-guidance-has-bounded-exit",
          "evidence": "observation",
          "outcome": "pass",
          "notes": "Observed first-hand at 2026-09-04: the served snapshot is dated (heading shows 'Quinta-Feira, 20 De Agosto' vs today 2026-09-04) and the surface presents that date openly next to per-review 'ATRASADA overdue Nd' badges derived from the snapshot asOf 2026-08-21 — staleness is visible, not disguised. The facilitator can stop rather than present stale guidance as current precisely because the asOf date and overdue deltas are on-screen; the facilitator guide's dojoToday section prescribes the regeneration procedure via the engine-owned prebuild, and no completion/mastery claim depends on the stale view (screenshot ev/dt-1-today.png)."
        }
      ]
    }
  ]
}
