{
  "name": "pixel-dojo",
  "private": true,
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "biome check pixel-quest/src pixel-quest/playwright shared",
    "build": "pnpm -r --if-present build",
    "smoke": "pnpm -r --if-present smoke"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.0",
    "@types/three": "~0.182.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.7",
    "vitest": "^4.0.15"
  },
  "packageManager": "pnpm@9.15.9"
}
