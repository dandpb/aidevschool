{
  "name": "17_distributed_config_service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4417 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4417",
    "test": "vitest run",
    "smoke": "playwright test",
    "lint": "biome check src playwright"
  },
  "dependencies": {
    "three": "~0.182.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.10.0",
    "@types/three": "~0.182.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.7",
    "vitest": "^4.0.15"
  }
}
