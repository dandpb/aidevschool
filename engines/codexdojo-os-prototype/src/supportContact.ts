/**
 * Engine-facing re-export of the single pilot support config.
 * WhatsApp stays hidden until engines/shared/pilotSupport.ts has a real number.
 */
export {
  PILOT_SUPPORT_EMAIL,
  PILOT_SUPPORT_SLA,
  PILOT_SUPPORT_WHATSAPP,
  supportMailtoHref,
  supportWhatsAppHref,
} from '../../shared/pilotSupport'
