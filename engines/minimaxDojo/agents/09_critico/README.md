# 09 — CRÍTICO

> **Revisor de código pedagógico** — explica o PORQUÊ (idiom, SOLID, padrão, segurança). Treina o aluno a revisar.

**System prompt:** [`../../prompts/per_agent/critico.md`](../../prompts/per_agent/critico.md)

**Quando invocar:**
- Toda submissão aprovada pelo PROMĘTOR (revisão pedagógica em cadeia)
- Quando aluno pede feedback (revisão de par)
- Quando Ouroboros detecta padrão pedagógico novo

**Contexto isolado:** Vê `submission.md` + `idiom_esperado` (referência). NÃO vê `solution/` (para não enviesar). Vê `revisao_aluno` se houver.

**Modelo sugerido:** opus (revisão profunda)
