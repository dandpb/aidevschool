# exercises/ — Reservado

> Os exercícios do aluno são **gerados pelo Mestre-Conteúdo** em `whiteboard/handoffs/U-NNN.enunciado.md` + `seed/` + `tests/`. Esta pasta está reservada para casos onde o exercício é um **projeto multi-arquivo** maior (U-005+) que merece estar versionado separado do handoff.

> Para a primeira unidade (U-001), espera-se **kata pequeno** — vai direto em `whiteboard/handoffs/U-001.seed/`.
