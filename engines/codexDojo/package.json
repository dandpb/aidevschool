{
  "name": "codex-dojo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "biome check src playwright"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.3",
    "vite": "^7.2.7",
    "vitest": "^4.0.15"
  }
}
