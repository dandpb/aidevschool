{
  "name": "codex-dojo-ds",
  "version": "0.1.0",
  "description": "codexDojo design system — warm-industrial mission control. React wrappers over the dashboard's CSS.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/ds.css dist/ds.css",
    "smoke": "pnpm run build && node scripts/smoke.mjs"
  },
  "peerDependencies": {
    "react": "^19"
  },
  "devDependencies": {
    "@types/react": "^19",
    "react": "^19",
    "react-dom": "^19",
    "typescript": "^5.9.3"
  }
}
