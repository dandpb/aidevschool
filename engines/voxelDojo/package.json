{
  "name": "voxel-dojo",
  "private": true,
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "biome check .",
    "dev:catalog": "node scripts/serve-catalog.mjs",
    "build": "pnpm -r --if-present build",
    "smoke": "pnpm -r --if-present smoke"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@playwright/test": "^1.49.0",
    "@types/three": "^0.170.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.15.9"
}
