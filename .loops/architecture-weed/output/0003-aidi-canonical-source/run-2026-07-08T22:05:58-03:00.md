# Run Output — 2026-07-08T22:05:58-03:00

Run-output artifact for the second recorded run of `architecture-weed` (after 2026-07-05). Records what was found, what
was decided, and what to look at next — so the next run starts from "what was the state last time."

## Substrate re-anchor (Step 1)

Could NOT skip Step 1: although the last run (2026-07-05) was within 7 days, canonical files changed
since — ADR-0002 was created 2026-07-08, and `AGENTS.md` was regenerated at commit `2591bdb`
(2026-07-08). Re-read fresh: `docs/FUNDAMENTOS.md`, `docs/PROMPTS/-01_GOAL.md`, `AGENTS.md` (root),
`docs/AGENTS.md`, `engines/codexDojo/ecosystem/MANIFEST.md`, prior ADRs (0001, 0002),
`docs/ARCHITECTURE_EVALUATION_2026-07-05.md`, and the new `docs/TECH_DEBT_AUDIT_2026-07-08.md`.

## Divergence list (this run)

Each entry: substrate claim → observed reality (file:line) → classification.

1. **GAP 3 (ciclo 01 stuck at `impl-done`)** — substrate claim: pipelines progress.
   Reality: **RESOLVED**. `learner/pipeline_status.md` now `cycle_id:
   2026-07-06-02-key-value-store`, `phase: cycle-complete`, `awaiting: next-curator`. Projects 01
   AND 02 both ran the full 5-phase cycle to completion (Node-only, honestly caveated in
   `curriculum/BACKLOG_STATUS.md`). The 2026-07-05 next-run hint is done.
   Classification: resolved — do not re-pick.

2. **Audit #1 (P32) — non-atomic canonical write** — claim: canonical state writes are safe.
   Reality: **RESOLVED at HEAD**. `engines/pixelDojo/verifier/__init__.py:313` routes through
   `commit_canonical` (`learner/substrate/__init__.py:98`) → `save_canonical` → atomic write
   (`learner/substrate/fsio.py:16`, temp + `os.replace`). Commit `b840234` ("deepen gate write
   seam") introduced the seam. The audit's `:320 plain .write_text()` is stale.
   Classification: resolved.

3. **Audit #4 (P24) — voxelDojo 15/16 stub reviewSlice** — claim: F2 (derived views regenerated).
   Reality: **RESOLVED at HEAD**. `sync_voxel_review_slice` (`dashboard_snapshot.py:303-320`) fans
   out to all 16 games; `test_voxel_slice.py:30` asserts `len(paths) >= 16`; all 16 files
   byte-identical md5 (`05b406a5…`); test green (3 passed).
   Classification: resolved.

4. **Audit #21 (P24) — drift tests failing at HEAD, alarm ignored** — claim: F7 (failure visible).
   Reality: **RESOLVED at HEAD**. `TestBacklogStatusDrift` + `picks_up_backlog_counts` → 4 passed.
   Commits `3244a86`/`cc2b618` (same day) fixed the counts and updated the tests.
   Classification: resolved.

5. **Audit #5 (P24) — AIDI derived views disagree, no canonical** — claim: F2 (single source).
   Reality: **CONFIRMED LIVE at HEAD, sharper than the audit** — 3 distinct live values (0.34 /
   0.5 / 0.0) + a 0.50 seed; full file:line evidence in ADR-0003 §Contexto + §Evidência.
   Classification: **spec bug (no canonical contract) + code bug (hardcoded adapter default +
   synthetic trendline presented as measurement)**. → **CHOSEN this run.**

## Divergences NOT chosen (live but weaker)

- **Audit #9 (P18) — `learner/substrate.validate()` is partial** (no checks on `empirical_gate`,
  `active_unit↔units_log` cross-check, attempt/evidence file existence). Consequential — it's the
  GAP-1 backstop — but the fix is "add invariant checks", a work item, not a fork in the road.
  Deprioritized vs #5's cleaner decision-worthiness. **Next-run candidate.**
- **`atomic_write_text` duplication** — `learner/substrate/fsio.py:16` + `engines/openclaw/fsio.py:15`.
  Leftover dup after the gate-seam work. Trivial F8 collapse, thin ADR. Low priority.
- **ADR-0002 produced outside this loop's memory** (no run-output, no memory entry; decisor line
  says "architecture-deepening loop (scheduled fire)"). Meta-process drift: two ADR-producing
  mechanisms don't share memory. Live but low-consequence; flag only.
- **MANIFEST count drift** — says "15 spatial-concept games implemented" but enumerates 16 (and
  `ls` shows 16). Cosmetic doc bug.

## Chosen divergence + decision

- **Divergence:** AIDI has no canonical source; 3 live views show 3 different values (0.34 / 0.5 /
  0.0) + a 0.50 seed, while two views falsely claim to derive from `learning_state.yaml` (which
  lacks the field). Violates F2; plus an F3/F7 angle (synthetic trendline presented as measurement).
- **Principle:** F2 (uma fonte da verdade, muitas views derivadas).
- **Decision (per Daniel's confirmation this run):** Option A — add canonical `learner.aidi` block
  to `learning_state.yaml`; dashboard already reads it (`:201`); whiteboard adapter derives instead
  of hardcoding; retire the synthetic trendline. Event-computed (Option B) is the revisit-when.

## Verifier verdict (fresh-context subagent, F3)

- score: **9/10** (bar was 9 — the ADR changes a substrate contract)
- result: **PASS** · done_rule: **PASS** · confidence: **high**
- failed_criteria: **none** · retry_target: **none**
- The verifier independently opened every cited file:line and confirmed each anchor (it even
  substantiated "consequential" via `learning_state.yaml:6` — the learner's goal is literally
  "robust professional-quality code without AI dependency").
- One non-blocking nit: the "4 values" framing slightly overcounts (0.5 == 0.50; event-log is a
  seed, not a live view). **Applied post-verification as a one-line honesty fix** (Context now says
  "3 distinct values + a seed"). The fix only makes the claim more conservative; it cannot lower
  the score. Recorded here for F4 auditability.

## Step 5 (regenerate derived views)

Skipped — decision-only run; no `learning_state.yaml` mutation, so no view regeneration is owed.
Implementation follow-up tracked in §Next-run hint.

## Next-run hint

1. **Implementation follow-up for ADR-0003** (highest value next): execute the ADR-0003 §Decisão
   contract — add the `learner.aidi` block, rewire `whiteboard.py:42` to derive, retire the
   synthetic trendline (`dashboard_snapshot.py:206-212`), run `python3 -m learner.substrate`,
   confirm dashboard ↔ whiteboard agree. Code change, not a new ADR; the loop can verify it didn't
   reintroduce divergence.
2. **Audit #9 (partial `validate()`)** is the next consequential *decision-worthy* divergence
   candidate — worth an ADR on "which invariants are load-bearing for the GAP-1 class" (e.g.
   `active_unit.state==mastered` ⇒ required attempt/evidence files exist).
3. **Re-verify the audit is still stale** — `TECH_DEBT_AUDIT_2026-07-08.md` items #1/#4/#21 are
   resolved; if a subsequent audit run is produced, cross-check before citing it (this run caught
   three stale "open" items).
4. Training Mode stays ON — this is clean verifier PASS **#1** (the 2026-07-05 run was
   `pending_verification`); bar is 3 consecutive. Do NOT graduate yet.
