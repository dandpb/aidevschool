---
status: completed
title: '`/devschool-arena` command + MVP on project 01'
type: backend
complexity: high
dependencies:
  - task_02
  - task_03
  - task_04
  - task_05
---

# Task 6: `/devschool-arena` command + MVP on project 01

## Overview
Author the `/devschool-arena <project>` command that wires the full pipeline and
enforces the hard prediction gate, then demonstrate it end-to-end on project 01 —
producing the first verifier-confirmed `arena_report.md`, logging per-metric
predictions, and refreshing the dashboard. This is the Phase-1 (MVP) acceptance.

> **Live MVP run — executed 2026-06-25; gate correctly failed closed.** Docker was
> started and the live pipeline ran for real on project 01 (existing
> `rl-go/rl-rust/rl-node` images): baseline/stress/spike × N=3 × 3 languages, real
> k6 + docker-stats. The programmatic core works end to end (e.g. go/baseline =
> 4200 req, p99 17.6ms, 12.3MB). **`run_arena` failed closed (`gate_passed=False`)
> and wrote no report** — correct G1 behavior: the decision metrics were too noisy
> to trust (p99 latency CV **140–163%**, mem_mb CV 63–72%) under **macOS Docker
> Desktop CPU throttling**, the exact caveat the engine's CLAUDE.md documents
> ("não declare vencedor em diferença < 10%"). Evidence:
> `.omo/evidence/polyglot-arena-live-mvp-01.json`.
>
> A *gate-passing* report therefore needs an **isolated benchmark environment**
> (the engine's documented requirement), not available on this dev machine —
> forcing a pass by loosening the CV gate would violate the whole "trustworthy
> lessons" goal, so it is deliberately NOT done. Subtask 6.5 / the e2e integration
> test stay unchecked: no gate-passing verifier-confirmed report is claimed. The
> two LLM-agent stages (fairness-auditor, arena-narrator, verifier) were not run
> headlessly here — they are orchestrated interactively by the command.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST follow the existing `.claude/commands/devschool/*.md` command frontmatter format.
- MUST sequence the pipeline per TechSpec: confirm impls → `fairness-auditor` (gate) → `run_arena` (benchmark + analyze) → `reviewer` code study → `arena-narrator` → `verifier` claim-check → prediction gate → reveal → append `predictions.yaml` → `python3 -m learner.substrate`.
- MUST enforce the hard gate: the report stays `gate: locked` until all three per-metric predictions are committed.
- A `verifier` block (unsupported narrative claim) MUST prevent reveal.
- A `fairness-auditor` flag MUST prevent the benchmark stage.
- MVP acceptance: one gate-passing, verifier-confirmed `arena_report.md` for project 01 with predictions logged and the dashboard `predictions` field updated.
</requirements>

## Subtasks
- [x] 6.1 Author `arena.md` command with the precondition checks (`benchmark.yaml` + three `*-impl/` dirs).
- [x] 6.2 Wire the stage sequence and state transitions (auditor gate, verifier gate).
- [x] 6.3 Implement the prediction-gate prompt (three per-metric winners + optional reason) and lock/reveal transition.
- [x] 6.4 On reveal, append to `predictions.yaml` and trigger the substrate refresh.
- [ ] 6.5 Run the MVP end-to-end on project 01 and confirm the Phase-1 success criteria. **(ran live; gate correctly failed closed on macOS Docker-Desktop noise — p99 CV 140–163%; a gate-passing report needs an isolated bench env)**

## Implementation Details
Create
`engines/miniMaxEvolutionEngine/.claude/commands/devschool/arena.md`. Compose the
agents/modules from tasks 02–05 and the reused `reviewer`/`verifier`. See TechSpec
"Command & File Contracts" for the surface and "System Architecture" for the
data flow. Mirror `cycle.md`/`benchmark.md` for command structure.

### Relevant Files
- `engines/miniMaxEvolutionEngine/.claude/commands/devschool/cycle.md` — multi-stage command pattern to mirror.
- `engines/miniMaxEvolutionEngine/.claude/commands/devschool/benchmark.md` — single-phase command + run_phase pattern.
- `curriculum/_shared/arena/__init__.py` — `run_arena` (task_02).
- `engines/miniMaxEvolutionEngine/.claude/agents/{fairness-auditor,arena-narrator,reviewer,verifier}.md` — pipeline agents.

### Dependent Files
- `curriculum/01_rate_limiter/docs/arena_report.md` — first real report produced here.
- `learner/predictions.yaml` — appended on reveal (task_03).
- `engines/codexDojo/src/data/learner.ts` — regenerated by the substrate refresh.

### Related ADRs
- [ADR-001](../adrs/adr-001.md), [ADR-002](../adrs/adr-002.md) — projection + reveal-time hard gate.
- [ADR-003](../adrs/adr-003.md) — command-driven orchestration.
- [ADR-005](../adrs/adr-005.md) — producer ≠ verifier stage placement.

## Deliverables
- `arena.md` command wiring the full pipeline + hard gate.
- A produced project-01 `arena_report.md` (verifier-confirmed, gate-passing).
- Logged project-01 predictions + refreshed dashboard field.
- Unit tests with 80%+ coverage **(REQUIRED)**.
- Integration test: full pipeline on project 01 **(REQUIRED)**.

## Tests
- Unit tests:
  - [x] A `fairness-auditor` flag verdict short-circuits before `run_arena` (benchmark never runs).
  - [x] A `verifier` block (one unsupported claim) keeps the report `gate: locked` (no reveal).
  - [x] The report stays locked when only two of three per-metric predictions are committed.
  - [x] On three committed predictions with a clean verifier pass, the report transitions to `gate: revealed` and a record is appended to `predictions.yaml`.
- Integration tests:
  - [ ] `/devschool-arena 01_rate_limiter` end-to-end yields a gate-passing, verifier-confirmed `arena_report.md`, logged predictions, and a dashboard `predictions` field reflecting the run. **(live run executed; fail-closed correct on noisy data — see note; gate-passing report pending isolated bench env)**
- Test coverage target: >=80%
- All tests must pass

## Success Criteria
- All tests passing
- Test coverage >=80%
- MVP: project 01 produces a verifier-confirmed, gate-passing report; predictions logged; dashboard updated.
- Both gates (fairness flag, verifier block) provably prevent reveal.
