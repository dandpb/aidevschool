# Integration contract

Main must contain the context-authority commits plus the pending product-code
changes: learner/substrate/gate.py with its existing new regression test and
the OS analytics events.ts consolidation. Preserve both sets of domain-map,
glossary and MANIFEST edits when merging.

No canonical learner/pipeline data or generated source is edited. Draft domain
plans keep their explicit proposed/unapproved status. The new ambiguity-analysis
findings are recommendations, not authorization to implement more behavior.
